# 本地开发环境配置
# 此文件已加入 .gitignore，不会提交到代码仓库

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/academic_nav?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 231101

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

# JWT 密钥（256 位随机字符串，仅用于本地开发）
jwt:
  secret: 7a9f3e2b1c8d4f6a0e5b9c3d7f1a2e4b6c8d0f2a4e6b8c0d2f4a6e8b0c2d4f6a
